globalThis.process ??= {}; globalThis.process.env ??= {};
const _107CreatingSeoFriendlyUrlsUsingHtaccess = "---\nimport BlogPost from \"../../layouts/BlogPost.astro\";\n\nconst title = \"Creating SEO friendly urls using htaccess\";\nconst description = \"Creating SEO Friendly URLs using .htaccess: Mastering URL Rewriting When developing a website, especially one aiming for strong Search Engine Optimization...\";\nconst date = \"2026-08-10\";\n---\n\n<BlogPost\n  title={title}\n  description={description}\n  date={date}\n>\n  <h1>Creating SEO Friendly URLs using .htaccess: Mastering URL Rewriting</h1>\n  <p>When developing a website, especially one aiming for strong Search Engine Optimization (SEO), clean, human-readable URLs are paramount. They improve user experience and signal better content relevance to search engines. The transition from cumbersome query string parameters to elegant path-based URLs is a common requirement, often managed through server configuration files like <code>.htaccess</code>.</p>\n  <p>The initial setup you described—using <code>index.php?p=page</code>—is functional but not SEO-friendly. We need to move towards routing that maps a clean URL directly to the desired content, such as <code>/home</code> instead of <code>/index.php?p=home</code>.</p>\n  <h2>Implementing Basic Front-Controller Rewriting</h2>\n  <p>The first step is setting up the basic rewrite rule. This rule tells the server how to handle requests that don't map directly to a physical file—it directs all traffic through a single entry point, typically <code>index.php</code>.</p>\n  <p>For a simple structure where you want <code>/home</code> to load content, you can use the following directive in your <code>.htaccess</code> file:</p>\n  <pre><code class=\"language-apache\">RewriteEngine On\n  RewriteCond %&#123;REQUEST_FILENAME&#125; !-f\n  RewriteCond %&#123;REQUEST_FILENAME&#125; !-d\n  RewriteRule ^([A-Za-z0-9-]+)/?$ index.php?p=$1\n  </code></pre>\n  <p>This rule effectively captures any request path (like <code>/home</code>) and redirects it internally to <code>index.php</code>, passing the requested segment (<code>home</code>) as a parameter (<code>p=home</code>). This achieves your first goal: making URLs cleaner.</p>\n  <h2>Moving Towards Dynamic, Friendly Slugs</h2>\n  <p>The challenge then shifts to creating truly friendly URLs, such as <code>/profile/john-doe</code>. This moves beyond simple path mapping and requires logic on how you structure your application routes. While <code>.htaccess</code> excels at basic URL manipulation (redirects, clean URLs), complex dynamic slug generation is usually handled within the application framework itself (like Laravel) where you define routes that map directly to controller methods.</p>\n  <p>However, we can use <code>.htaccess</code> to ensure the structure remains clean and handle potential fallback scenarios. If you are leveraging a modern MVC framework, such as one based on the principles found in <a href=\"https://laravelcompany.com\">Laravel</a>, route definition is usually handled programmatically rather than purely via <code>.htaccess</code>. The rewrite rules serve as an excellent foundation for ensuring that all traffic hits your main application entry point correctly before framework routing takes over.</p>\n  <h2>Handling Parameters and Unique Names</h2>\n  <p>Regarding your specific questions about integrating profile names:</p>\n  <ol>\n  <li><strong>Adding Friendly Names:</strong> To achieve URLs like <code>/profile/john-doe</code> instead of relying solely on IDs, you need a mechanism <em>before</em> the request hits the main application logic to parse the desired slug from the URL path. If the structure is <code>/profile/slug</code>, your routing system must be configured to look at the segment after <code>/profile/</code> and use that as the identifier for loading the correct resource (e.g., fetching the user with <code>slug=john-doe</code>).</li>\n  <li><strong>Unique Names vs. IDs:</strong> Using unique, descriptive names (slugs) like <code>john-doe</code> is vastly superior for SEO than using numeric IDs (<code>id=20</code>). Slugs are readable and convey meaning directly to both users and search engines.</li>\n  <li><strong>Impact on Existing URLs:</strong> If you successfully implement a clean routing system where the application handles all URL parsing, you generally <em>do not</em> need to manually update every link. Instead, you use template helpers or framework functions within your HTML views (e.g., <code>route('profile.show', $user)</code>) to generate these links dynamically. This keeps your code DRY (Don’t Repeat Yourself) and resilient to future URL changes.</li>\n  </ol>\n  <h2>Troubleshooting Asset Loading Issues</h2>\n  <p>You noticed that after applying the initial <code>RewriteRule</code>, CSS stylesheets and JavaScript files are not loading correctly. This is a very common issue when implementing URL rewriting, particularly if you are using absolute paths or if your assets are referenced relative to the old structure.</p>\n  <p>This typically happens because the server doesn't know how to resolve the path for static assets when routing through <code>index.php</code>. You need to ensure that your application correctly manages asset paths. In modern PHP frameworks, this is usually solved by defining a public asset directory and ensuring all asset loading calls use framework helper functions that generate correct, web-accessible URLs. For instance, if you are working within an established structure like Laravel, the framework provides robust tools for managing asset file paths, which bypasses manual <code>.htaccess</code> path conflicts for static files. Always ensure your root directory configuration is set up to serve assets correctly when routing through a front controller.</p>\n\n</BlogPost>\n";

export { _107CreatingSeoFriendlyUrlsUsingHtaccess as default };
